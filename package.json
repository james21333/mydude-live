{
  "name": "mydude-live",
  "version": "0.2.0",
  "private": true,
  "description": "Dynamic mydude.live AI ecosystem router and live avatar demo.",
  "type": "module",
  "scripts": {
    "build": "vite build && node scripts/validate.mjs",
    "deploy": "wrangler deploy",
    "test": "vite build && node scripts/validate.mjs",
    "dev": "vite --host 0.0.0.0"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "typescript": "latest",
    "react": "latest",
    "react-dom": "latest",
    "lucide-react": "latest"
  },
  "devDependencies": {
    "wrangler": "^4.92.0"
  }
}
